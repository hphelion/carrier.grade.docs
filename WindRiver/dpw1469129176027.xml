<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task-wr PUBLIC "-//WindRiver.com//DTD DITA 1.2 Wind River General Task//EN" "task-wr.dtd">
<task-wr id="dpw1469129176027" xml:lang="en-us">
<!-- Modification History
        
-->
    <title ixia_locid="1">Configuring the Login Timeout for the Web Administration Interface</title>
    <shortdesc ixia_locid="2">For security, login to the Web administration interface can be
        disabled for a user after several consecutive failed attempts. You can configure how many
        failed attempts are allowed before the user is locked out, and how long the user must wait
        before the lockout is reset.</shortdesc>
    <prolog>
        <author ixia_locid="3"/>
    </prolog>
    <taskbody>
        <context id="context_N1001E_N1001B_N10001" ixia_locid="4">
            <p ixia_locid="5"><note id="note_N10029_N10026_N10021_N1001E_N10001" ixia_locid="28" type="caution">
                    <p ixia_locid="29">This procedure requires the Web service to be restarted,
                        which causes all current user sessions to be lost. To avoid interrupting
                        user sessions, perform this procedure during a scheduled maintenance period
                        only. </p>
                </note><draft-comment author="jowens" ixia_locid="6">US77622 new topic; CGTS-4995
                    defaults updated</draft-comment>By default, after three consecutive failed login
                attempts, a user must wait five minutes (300 seconds) before attempting another
                login. During this period, all Web administration interface login attempts by the
                user are refused, including those using the correct password.</p>
            <p ixia_locid="30"><draft-comment author="jowens" ixia_locid="34">CGTS-4995 names
                    changed</draft-comment>This behavior is controlled by the <parmname ixia_locid="31">lockout_retries</parmname> parameter and the <parmname ixia_locid="32">lockout_seconds</parmname> service parameter. To review their
                current values, use the <cmdname ixia_locid="33">system
                    service-parameter-list</cmdname> command.</p>
            <p ixia_locid="9">You can change the duration of the lockout using the following CLI command:</p>
            <codeblock ixia_locid="10"><systemoutput ixia_locid="11">~(keystone_admin)$ </systemoutput><userinput ixia_locid="12">system service-parameter-add horizon auth \
lockout_seconds=<varname ixia_locid="13">duration</varname></userinput></codeblock>
            <p ixia_locid="14">where <varname ixia_locid="15">duration</varname> is the time in seconds.</p>
            <p ixia_locid="16">You can change the number of allowed retries before a lockout is imposed using the
                following CLI command:</p>
            <codeblock ixia_locid="17"><systemoutput ixia_locid="18">~(keystone_admin)$ </systemoutput><userinput ixia_locid="19">system service-parameter-add horizon auth \
lockout_retries=<varname ixia_locid="20">attempts</varname></userinput></codeblock>
            <p ixia_locid="21">where <varname ixia_locid="22">attempts</varname> is the number of allowed retries.</p>
            <p ixia_locid="23">For the changes to take effect, you must apply them:</p>
            <codeblock ixia_locid="24"><systemoutput ixia_locid="25">~(keystone_admin)$ </systemoutput><userinput ixia_locid="26">system service-parameter-apply horizon</userinput></codeblock>
            <p ixia_locid="27">Allow about 30 seconds after applying the changes for the Web service
                to restart.</p>
        </context>
    </taskbody>
</task-wr>