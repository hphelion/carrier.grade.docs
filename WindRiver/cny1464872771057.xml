<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept-wr PUBLIC "-//WindRiver.com//DTD DITA 1.2 Wind River Concept//EN" "concept-wr.dtd">
<concept-wr id="cny1464872771057" xml:lang="en-us">
<!-- Modification History

 -->
 <title ixia_locid="1">Dedicated Storage for Systems Using Controller Storage</title>
 <shortdesc ixia_locid="2">On systems that do not already have it, you can add dedicated Ceph
  storage for Cinder volumes and Glance images at any time after installation.</shortdesc>
 <prolog>
  <author ixia_locid="20">Jim Owens</author>
  <author ixia_locid="3">Edward Knowlton</author>
 </prolog>
 <conbody>
  <draft-comment author="eknowlto" ixia_locid="18">US77367 / US78833 Add CEPH after config
   controller </draft-comment>
  <p ixia_locid="21">Titanium Server can be configured at installation to use different types of
   storage backends. If you choose to install it with a backend other than Ceph, you can add Ceph
   storage later. This provides an additional backend for Cinder volumes and Glance images.</p>
  <p ixia_locid="36">If a Ceph backend is present, it is used by default for new images or volumes.
   Existing volumes and images are unaffected by the addition of a Ceph backend, and continue to use
   their existing backends.</p>
  <p ixia_locid="45">For a new image, you can specify a different backend manually by including the
    <parmname ixia_locid="38">--store</parmname> parameter with the <cmdname ixia_locid="39">glance
    image-create</cmdname> command, and setting its value to <option ixia_locid="40">file</option>
   (for an LVM backend) or <option ixia_locid="41">rbd</option> (for a Ceph backend).</p>
  <p ixia_locid="46">For a new volume, you can specify a backend manually by referencing a volume
   type when using <cmdname ixia_locid="42">cinder create</cmdname>. The volume type must be defined
   in advance using <cmdname ixia_locid="43">cinder type-create</cmdname>, and then associated with
   a backend using <cmdname ixia_locid="44">cinder type-key</cmdname>. For complete information,
   refer to the public OpenStack documentation.</p>
  <p ixia_locid="7">To add Ceph storage to a system, you must use CLI commands. This operation is
   not supported from the Web administration interface.</p>
  <p ixia_locid="32">As part of the new Ceph storage, a Ceph monitor logical volume is created. By
   default, it uses space on the rootfs disk of the controller. If the rootfs disk does not have
   enough free space, an additional disk is required (the disk assigned for Cinder volumes cannot be
   used for this purpose). To add disks to a controller, see <xref href="gfu1465307467334.xml" ixia_locid="29"/>.</p>
  <p ixia_locid="35">The size of the Ceph monitor volume is shown in the <nameliteral ixia_locid="10">CEPH Mon Storage (GiB)</nameliteral> field on the <uicontrol ixia_locid="14">Controller Filesystem</uicontrol> tab, accessible from the <menucascade ixia_locid="19">
    <uicontrol ixia_locid="11">Admin</uicontrol>
    <uicontrol ixia_locid="12">System</uicontrol>
    <uicontrol ixia_locid="13">System Configuration</uicontrol>
   </menucascade> page of the Web administration interface. You can change the size using the CLI or
   the Web administration interface. For more information, see <xref href="ers1464645579505.xml" ixia_locid="23"/>.</p>
  <p ixia_locid="30">Ceph storage also requires an infrastructure network. To add one, see <xref href="jow1420473404578.xml" ixia_locid="31"/>.</p>
  <note id="note_N10068_N10023_N10001" ixia_locid="25">
   <p ixia_locid="17">You cannot add Ceph storage to a Titanium Server CPE system.</p>
  </note>
 </conbody>
</concept-wr>